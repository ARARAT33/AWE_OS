[package]
name = "aweos-kernel"
version = "0.2.1"
edition = "2024"
publish = false

[lib]
path = "src/lib.rs"

[dependencies]
awe-boot-protocol = { workspace = true }

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
opt-level = 3
lto = "fat"
codegen-units = 1
